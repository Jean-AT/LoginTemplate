export default function ChangeData(){
    if(!sessionStorage.getItem("accessToken")){
        console.log("Ustede no tiene acceso")
    }else{
        console.log("usted tiene acceso")
    }

    return(
        <dev>

        </dev>
    )
}